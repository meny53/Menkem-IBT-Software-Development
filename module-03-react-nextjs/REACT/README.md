# React Day 1 - JSX and Components

This is my Day 1 React project. It covers the basics of setting up React with Vite, JSX, components, props, and rendering lists with the map() function.

## What I learned

- Setting up a React project with Vite
- JSX
- React components
- Props
- Destructuring props
- Rendering arrays with map()
- Using unique keys

## How to run

Install the dependencies:

```bash
npm install